/**
 * Archivo: app.js
 * Propósito: Lógica de frontend (Vanilla JS / Lógica de Interfaz cliente-servidor)
 * Carga de datos base en la interfaz o rutinas estáticas de apoyo.
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log("Sistema de Gestión TCS - Interface Web levantada correctamente.");
});
